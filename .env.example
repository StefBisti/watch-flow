# LOCAL DEV VALUES

# --- Database (local docker-compose) ---
DATABASE_URL=postgresql://watchflow:watchflow@localhost:5432/watchflow
DIRECT_URL=postgresql://watchflow:watchflow@localhost:5432/watchflow
WORKER_DATABASE_URL=postgresql://watchflow:watchflow@localhost:5432/watchflow

# --- Redis (local docker-compose) ---
REDIS_URL=redis://localhost:6379

# --- Auth (Auth.js) ---
AUTH_URL=http://localhost:3000
AUTH_SECRET=
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# --- Email (Resend) ---
RESEND_API_KEY=
EMAIL_FROM=WatchFlow <onboarding@resend.dev>

# --- Crypto (worker only) ---
SECRETS_MASTER_KEY=
IP_HASH_SALT=

# --- App ---
NEXT_PUBLIC_APP_URL=http://localhost:3000

# --- Observability ---
SENTRY_DSN=

# --- Rate limiting (Upstash) ---
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=